import { chapter } from "./chapter";
import { photo } from "./photo";
import { section } from "./section";

// Order here drives the Studio's default document-type list.
export const schemaTypes = [section, chapter, photo];
